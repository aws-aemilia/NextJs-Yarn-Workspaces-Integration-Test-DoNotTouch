version: 1
applications:
  - appRoot: packages/web-app
    frontend:
      phases:
        preBuild:
          commands:
            - cd ../../
            - yarn install
        build:
          commands:
            - cd packages/web-app
            - yarn workspace @project/web-app build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'